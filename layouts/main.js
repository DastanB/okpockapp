import React from 'react'

const Page = ({children}) => (
  <html>
      <head></head>
      <body>
          {children}
      </body>
    <style jsx>{`
    `}</style>
  </html>
)

export default Page
